package crypto_test

import (
	"testing"
)

func TestFilterSensitiveWords(t *testing.T) {
	//content := "中华人民共和国万岁，毛ze东领导了文化大革命。天an安门事件"
	//c := []rune(content)
	//t.Log(len(content), len(c), string(c[len(c)-1]))
	//fw := crypto.FilterSensitiveWords(c)
	//t.Logf("%s\n", string(fw))
}
